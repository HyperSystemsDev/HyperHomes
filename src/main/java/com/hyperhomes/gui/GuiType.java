package com.hyperhomes.gui;

/**
 * Enum of all GUI page types in HyperHomes.
 * Used by ActivePageTracker to track which pages players have open.
 */
public enum GuiType {
    HOMES,
    HOME_DETAIL,
    SHARED,
    SHARE_MANAGE,
    SHARE_CONFIRM,
    ADMIN,
    ADMIN_SETTINGS
}
